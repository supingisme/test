https://pmem.io/
--------------------------------------

术语“持久性存储器”用于描述允许程序将数据访问为可直接按字节寻址的存储器的技术，而其内容是非易失性的，可在重启后保留。它具有类似于内存的方面和类似于存储的方面，但是通常不会替换内存或存储。相反，持久性内存是第三层，与内存和存储一起使用。

有了这种新成分，包含持久性内存的系统就可以超越传统配置，从而提供更快的启动时间，更快地访问大型内存数据集，并且通常可以降低总拥有成本。
