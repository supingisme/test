MSI - Message Signaled Interrupt

当设备向一个特殊地址写入时，会向CPU产生一个中断，即为 MSI中断。
MSI最初在 PCI 2.2 里定义，在 PCI 3.0 中被强化，使得每个中断都可以
单独控制。
PCI 3.0 还引入了 MSI-X 能力，相比MSI，每个设备可以支持更多的中断，
并可以独立配置。
--------------------------------------------

